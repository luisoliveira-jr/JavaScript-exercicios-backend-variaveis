// Calculo do perímetro e área de um círculo
// 

const pi = 3.14;
let raio = 1;

let comprimento = 2 * pi * raio;

let area = pi * (raio ** 2);

console.log(`Se temos raio = ${raio}, o comprimento da circunferência será de ${comprimento} e a sua área será equivalente a ${area}.`);